<h1>Sign Up</h1>

<%= form_for @user do |u| %>
	<%= u.label :username %>
	<%= u.text_field :username %>
	<%= u.label	:email %>
	<%= u.text_field :email %>
	<%= u.label :password %>
	<%= u.text_field :password %>
	<%= u.submit :create %>

<% end %>